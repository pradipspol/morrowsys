# morrowsys

React + Vite rebuild of the MorrowSys landing site.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The site uses hash routing so the FocusKube page works on static hosting.
